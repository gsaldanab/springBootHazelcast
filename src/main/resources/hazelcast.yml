hazelcast:
  network:
    join:
      multicast:
        enabled: true
  map:
   count:
     time-to-live-seconds: 10
     max-idle-seconds: 10
     eviction:
       eviction-policy: LRU
       max-size-policy: PER_NODE
       size: 0